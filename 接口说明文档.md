# 干扰系统接口说明文档

本文档描述用于干扰系统运维控制的 HTTP 接口，包括状态查询与强制重置能力。

## 基础信息

- 服务基地址：`http://<host>:<port>`（默认 `0.0.0.0:17686`）
- 返回格式：`application/json`
- 时间字段格式：`YYYY-MM-DD HH:MM:SS`

### 启动地址配置（可选）

- 支持通过环境变量配置监听地址：
  - `HOST`：监听 IP（默认 `0.0.0.0`）
  - `PORT`：监听端口（默认 `17686`）
- 示例（PowerShell）：

```powershell
$env:HOST="0.0.0.0"
$env:PORT="18000"
python app.py
```

说明：

- 当前服务优先使用 `waitress` 启动，避免 Flask 开发服务器的警告输出。
- 若环境中未安装 `waitress`，会自动回退到 Flask 内置开发服务器。

## 1) 健康检查

- 路径：`/api/health`
- 方法：`GET`
- 描述：检查服务进程是否存活

### 成功响应（200）

```json
{
  "status": "ok",
  "timestamp": "2026-04-28 11:00:00"
}
```

## 2) 查询系统状态

- 路径：`/api/status`
- 方法：`GET`
- 描述：查询当前干扰系统状态、关联项目与错误信息

### 字段说明

- `status`：系统状态
  - `idle`：空闲
  - `running`：正在处理某个项目的数据流
  - `error`：出现异常（可结合 `error` 字段查看）
- `project`：当前关联项目名（无则为 `null`）
- `error`：最近一次错误信息（无则为 `null`）
- `flow_stage`：当前流程阶段快照，包含项目名及部署阶段完成情况
- `last_update_time`：最近一次状态、流程或帧处理更新时间
- `frame_count`：当前流已接收的有效融合帧数量
- `timestamp`：响应时间

### 空闲响应示例（200）

```json
{
  "status": "idle",
  "project": null,
  "error": null,
  "flow_stage": null,
  "last_update_time": "2026-04-28 11:00:00",
  "frame_count": 0,
  "timestamp": "2026-04-28 11:00:00"
}
```

### 运行中响应示例（200）

```json
{
  "status": "running",
  "project": "test_project_001",
  "error": null,
  "flow_stage": {
    "project": "test_project_001",
    "generate_done": true,
    "regenerate_done": true
  },
  "last_update_time": "2026-04-28 11:05:01",
  "frame_count": 3,
  "timestamp": "2026-04-28 11:05:00"
}
```

### 异常响应示例（200）

```json
{
  "status": "error",
  "project": "test_project_001",
  "error": "通信随机 增量更新失败: position index out of range",
  "flow_stage": {
    "project": "test_project_001",
    "generate_done": true,
    "regenerate_done": true
  },
  "last_update_time": "2026-04-28 11:10:03",
  "frame_count": 8,
  "timestamp": "2026-04-28 11:10:00"
}
```

## 3) 强制终止/重置

- 路径：`/api/reset`
- 方法：`POST`
- 描述：立即终止当前流式任务并重置系统到空闲状态
- 请求体：无

### 成功响应（200）

```json
{
  "success": true,
  "message": "已终止当前任务，系统已重置为空闲状态"
}
```

### 失败响应（500）

```json
{
  "success": false,
  "message": "重置失败: <错误详情>"
}
```

## 状态流转说明

- 新项目流初始化成功：`idle -> running`
- 流式处理结束（`simStop`、超时最终化、手动 reset）：`running -> idle`
- 流初始化失败或关键处理异常：`running -> error`
- reset 成功后统一回到 `idle`

## 消息处理顺序要求（新增）

服务端已增加流程闸门，融合数据不会“抢跑”，必须按以下顺序：

1. 先收到部署阶段消息（同一项目）：
   - `type=generate`（随机部署）
   - `type=reGenerate`（优化部署）
2. 收到融合数据帧即视为决策启动并开始处理：
   - `type=simData` 且 `sensor_type=AA00`

说明：

- 不再要求单独的决策开始帧（`simStart` / `decisionStart`）。
- 只要部署阶段完成，第一帧符合条件的融合数据到达即开始决策处理。
- 若部署阶段未完成，服务会记录 warning 并忽略该帧，不进入融合处理。

## 使用建议

- 运维监控可周期调用 `/api/status` 观察状态。
- 当状态为 `error` 且无法自恢复时，调用 `/api/reset` 进行人工干预。
- 启动干扰任务由 MQTT 消息自动驱动（`generate` / `reGenerate` / `simData`），无需提供 HTTP 启动接口。

## 本地联调脚本

项目内提供了四个测试脚本：

- `test_api.ps1`
  - 用于验证 HTTP 接口：`/api/health`、`/api/status`、`/api/reset`
- `test_mqtt_flow.py`
  - 用于验证 MQTT 正确流程：`generate -> reGenerate -> simData -> simStop`
  - 脚本会在每个关键步骤后自动查询 `/api/status`
  - 步数据响应会按类型汇总统计，避免终端刷屏
- `test_mqtt_flow_bad.py`
  - 用于验证 MQTT 错误顺序流程是否会被拦截
  - 支持两种错误场景：
    - `simdata_only`：未部署直接发送 `simData`
    - `generate_then_simdata`：只做 `generate`，未做 `reGenerate` 就发送 `simData`
- `run_all_tests.ps1`
  - 一键顺序执行 HTTP 测试、错误顺序测试、正确顺序测试
  - 执行结束后会输出测试结果汇总表

### `run_all_tests.ps1` 使用示例

先启动服务：

```powershell
python app.py
```

再执行：

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\run_all_tests.ps1
```

### `test_mqtt_flow.py` 使用示例

先启动服务：

```powershell
python app.py
```

再执行：

```powershell
python test_mqtt_flow.py
```

常用参数示例：

```powershell
python test_mqtt_flow.py --frames 5 --interval 0.5
python test_mqtt_flow.py --base-url "http://127.0.0.1:18000" --topic-deploy "vi_decision" --topic-sim "pg_data_processor_finish"
python test_mqtt_flow.py --project "faeef_test"
```

预期结果：

- 部署完成后，`simData` 到达时 `/api/status` 应变为 `running`
- 发送 `simStop` 后 `/api/status` 应回到 `idle`

### `test_mqtt_flow_bad.py` 使用示例

错误场景 1：未部署直接发送 `simData`

```powershell
python test_mqtt_flow_bad.py --mode simdata_only
```

错误场景 2：只做 `generate` 后直接发送 `simData`

```powershell
python test_mqtt_flow_bad.py --mode generate_then_simdata
```

预期结果：

- `/api/status` 应保持 `idle`
- 服务日志应出现“部署阶段未完成，忽略”之类 warning
