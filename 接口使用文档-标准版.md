# 干扰系统接口使用文档

## 1. 通用说明

1. 所有 HTTP 接口均返回 `JSON` 格式。
2. 当前服务接口并非全部为 `POST`，请以接口定义为准。
3. 当前已实现接口如下：
   1. `GET /api/health`
   2. `GET /api/status`
   3. `POST /api/reset`
4. 返回结构说明：
   1. 健康检查接口返回：
      - `status`：string，服务状态。
      - `message`：string，不健康时的原因说明。
      - `timestamp`：string，当前时间。
      - `details`：object，健康明细（如 MQTT 连接状态、系统状态）。
   2. 状态查询接口返回：
      - `status`：string，系统状态，取值为 `idle` / `running` / `error`。
      - `state_reason`：string，状态原因（如 `initial`、`normal_finished`、`timeout_finished`、`manual_reset`）。
      - `project`：string / null，当前项目名称。
      - `error`：string / null，异常信息。
      - `flow_stage`：object / null，当前流程阶段快照。
      - `last_update_time`：string，最近一次状态更新时间。
      - `frame_count`：int，已处理的融合帧数量。
      - `timestamp`：string，当前时间。
   3. 重置接口返回：
      - `success`：bool，是否成功。
      - `message`：string，提示信息。

## 2. 干扰系统运维控制

### 2.1. 健康检查

**接口描述**：检查当前服务进程是否存活。  
**请求方法**：`GET`  
**请求路径**：`/api/health`  
**请求参数**（无）：

**返回示例**：

```json
{
  "status": "ok",
  "timestamp": "2026-04-28 16:00:00",
  "details": {
    "mqtt_connected": true,
    "system_status": "idle",
    "state_reason": "initial"
  }
}
```

**不健康返回示例（HTTP 仍为 200，通过 body 中 `status: unhealthy` 区分）**：

```json
{
  "status": "unhealthy",
  "message": "MQTT disconnected",
  "timestamp": "2026-04-28 16:01:10",
  "details": {
    "mqtt_connected": false,
    "system_status": "idle",
    "state_reason": "initial"
  }
}
```

### 2.2. 干扰系统状态查询

**接口描述**：查询当前干扰系统运行状态、当前项目、异常信息、流程阶段和帧处理信息。  
**请求方法**：`GET`  
**请求路径**：`/api/status`  
**请求参数**（无）：

**返回字段说明**：

| 参数名           | 类型          | 必填 | 说明                                   |
| ---------------- | ------------- | ---- | -------------------------------------- |
| `status`         | `string`      | 是   | 系统状态：`idle` / `running` / `error` |
| `state_reason`   | `string`      | 是   | 状态原因（见下方说明）                 |
| `project`        | `string/null` | 否   | 当前项目名称                           |
| `error`          | `string/null` | 否   | 异常信息                               |
| `flow_stage`     | `object/null` | 否   | 流程阶段快照                           |
| `last_update_time` | `string`    | 是   | 最近一次状态更新时间                   |
| `frame_count`    | `int`         | 是   | 当前流已处理的融合帧数量               |
| `timestamp`      | `string`      | 是   | 当前服务时间                           |

**`state_reason` 说明**：

- `initial`：服务启动后的初始空闲状态
- `first_simdata_started`：接收到首帧有效融合数据并开始运行
- `normal_finished`：收到 `fusion_end`（`pg_data_processor_finish` 主题）或正常最终化后回到空闲
- `timeout_finished`：空闲超时触发最终化后回到空闲
- `manual_reset`：调用 `/api/reset` 后回到空闲
- `simulator_init_failed`：流式模拟器初始化失败
- `frame_update_failed`：处理融合帧时更新失败
- `finalize_exception`：最终化阶段出现异常
- `manual_reset_failed`：手动重置失败

**返回示例（空闲）**：

```json
{
  "status": "idle",
  "state_reason": "initial",
  "project": null,
  "error": null,
  "flow_stage": null,
  "last_update_time": "2026-04-28 16:00:00",
  "frame_count": 0,
  "timestamp": "2026-04-28 16:00:00"
}
```

**返回示例（运行中）**：

```json
{
  "status": "running",
  "state_reason": "first_simdata_started",
  "project": "faeef",
  "error": null,
  "flow_stage": {
    "project": "faeef",
    "generate_done": true,
    "regenerate_done": true
  },
  "last_update_time": "2026-04-28 16:17:36",
  "frame_count": 3,
  "timestamp": "2026-04-28 16:17:37"
}
```

**返回示例（异常）**：

```json
{
  "status": "error",
  "state_reason": "frame_update_failed",
  "project": "faeef",
  "error": "通信随机 增量更新失败: position index out of range",
  "flow_stage": {
    "project": "faeef",
    "generate_done": true,
    "regenerate_done": true
  },
  "last_update_time": "2026-04-28 16:20:03",
  "frame_count": 8,
  "timestamp": "2026-04-28 16:20:03"
}
```

### 2.3. 启动干扰（原逻辑）

系统不提供 HTTP 启动接口。  
系统收到 MQTT 消息后，会自动进入相应处理流程。

消息顺序要求如下（二选一）：

**方式 A：完整部署**

1. `type=generate`（随机部署，可无 `projectname`；仅 MQTT 返回，不落盘）
2. `type=reGenerate`（优化部署，须带 `projectname`）
3. `type=simData` 且 `sensor_type=AA00`

**方式 B：跳过优化（jammerStart）**

1. `Type=jammerStart`（`Scene.projectName` + `Scene.range` + `Jammers` 优化后坐标）
2. 服务端按场景范围自动生成随机位置并落盘，同时保存优化位置
3. `type=simData` 且 `sensor_type=AA00`

说明：

1. 不再要求单独的 `simStart` 或 `decisionStart` 开始帧。
2. `jammerStart` 报文中 `Jammers` 坐标视为优化部署；随机位置由 `Scene.range` 圆心/半径（米）自动生成。
3. 决策从 `data/<projectName>/` 读取随机（`original*`）与优化（`optimized*`）两套位置。
4. 若部署阶段未完成，融合数据会被忽略。
5. 融合数据流结束：在 `pg_data_processor_finish` 主题收到 `type=fusion_end` 时，表示不再有融合数据，服务端立即触发最终化（无需解析 `endFlag` 等其它字段）。

### 2.4. 终止干扰 / 重置系统

**接口描述**：立即终止当前正在运行的任务，并将系统恢复为空闲状态。  
**请求方法**：`POST`  
**请求路径**：`/api/reset`  
**请求参数**（无）：

**成功返回示例**：

```json
{
  "success": true,
  "message": "已终止当前任务，系统已重置为空闲状态"
}
```

**失败返回示例**：

```json
{
  "success": false,
  "message": "重置失败: <错误详情>"
}
```

## 3. 状态流转说明

1. 初始状态：`idle`
2. 第一帧有效融合数据开始处理后：`running`
3. 正常结束、超时结束、手动重置后：`idle`
4. 处理异常时：`error`


决策数据库查询接口文档

两个接口均为 **POST** 请求，返回 **JSON** 格式。

一. 请求方法：两个接口均使用 **POST**，不能使用 GET。  
请求头：`Content-Type: application/json`。  
请求体：JSON 格式。  
响应体：JSON 格式。

统一响应外层结构：

```json
{
  "code": 200,
  "data": { ... },
  "msg": "查询成功"
}
```

`code`：业务状态码，`200` 表示成功。  
`data`：实际返回数据。  
`msg`：提示信息。

---

## 接口一：查询项目与批次列表

### 接口说明

从 `fusion.decision_result_simulation` 表中查询全部不重复的 `projectname` 和 `batchname` 组合，以表格形式返回。  
返回格式包含 `columnList`、`data`、`total`，与「获取所有场景接口」一致。  
本接口仅返回项目名称和批次名称两列，供接口二查询时使用。

### 请求信息

方法：**POST**  
路径：`/api/fusion/decision/process/list`

### 返回字段说明

`data.columnList`：列定义列表。  
`columnName`：列名（string）。  
`indexNumber`：列索引，从 0 开始（int）。  
`data.data`：二维数组，每一行是一条记录，列顺序与 `columnList` 一致。  
`data.total`：记录总数。

当前仅返回以下两个字段：  
`projectname`：项目名称。  
`batchname`：批次名称。

### 成功返回示例

```json
{
  "code": 200,
  "data": {
    "columnList": [
      {"columnName": "projectname", "indexNumber": 0},
      {"columnName": "batchname", "indexNumber": 1}
    ],
    "data": [
      ["3kmv888", "default"]
    ],
    "total": 1
  },
  "msg": "查询成功"
}
```

### 无数据时返回

表中没有记录时仍返回 `code: 200`，`data.data` 为空数组，`total` 为 0。

```json
{
  "code": 200,
  "data": {
    "columnList": [
      {"columnName": "projectname", "indexNumber": 0},
      {"columnName": "batchname", "indexNumber": 1}
    ],
    "data": [],
    "total": 0
  },
  "msg": "查询成功"
}
```

---

## 接口二：按项目与批次查询决策结果

### 接口说明

根据请求 Body 中的 `projectname` 和 `batchname`，查询 `fusion.decision_result_simulation` 表中匹配的全部记录。  
返回格式为对象列表，类似「获取正在运行的任务」接口中的 `executions` 列表。

### 请求信息

方法：**POST**  
路径：`/api/fusion/decision/result/query`

### 请求 Body

`projectname`：项目名称，必填，类型 string。  
`batchname`：批次名称，必填，类型 string。

请求示例：

```json
{
  "projectname": "3kmv888",
  "batchname": "default"
}
```

若不知道填什么，可先调用接口一，从返回的 `data.data` 中取第 1 列（projectname）和第 2 列（batchname）。

### 返回字段说明

`data.results`：结果对象数组，每条记录包含表中全部字段。  
`projectname`：项目名称。  
`batchname`：批次名称。  
`type`：类型。  
`status`：状态。  
`message`：消息。  
`timestamp`：时间戳。  
`CommunicationMetrics`：通信指标（文本，可能为 JSON 字符串）。  
`RadarMetrics`：雷达指标（文本）。  
`CommunicationMetricsFile`：通信指标文件路径。  
`RadarMetricsFile`：雷达指标文件路径。  
`data.total`：匹配记录数。

### 成功返回示例

```json
{
  "code": 200,
  "data": {
    "results": [
      {
        "projectname": "3kmv888",
        "batchname": "default",
        "type": "123",
        "status": "213",
        "message": "fsfefs",
        "timestamp": "2026-06-04 14:27:16",
        "CommunicationMetrics": "...",
        "RadarMetrics": "...",
        "CommunicationMetricsFile": "...",
        "RadarMetricsFile": "..."
      }
    ],
    "total": 1
  },
  "msg": "查询成功"
}
```

### 无数据时返回

数据库中没有匹配记录时，仍返回 `code: 200`，`results` 为空数组，`total` 为 0。

```json
{
  "code": 200,
  "data": {
    "results": [],
    "total": 0
  },
  "msg": "查询成功"
}
```

### 参数错误返回

缺少 `projectname` 或 `batchname` 时，HTTP 状态码为 400。

```json
{
  "code": 400,
  "data": {
    "results": [],
    "total": 0
  },
  "msg": "projectname 和 batchname 不能为空"
}
```
